<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DziennikPlecakowy.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:DziennikPlecakowy.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="DziennikPlecakowy"
    
    Shell.TabBarBackgroundColor="{AppThemeBinding Light={StaticResource LightCardBackground}, Dark={StaticResource DarkCardBackground}}"
    
    Shell.TabBarUnselectedColor="{AppThemeBinding Light={StaticResource LightPlaceholder}, Dark={StaticResource DarkPlaceholder}}"
    
    Shell.TabBarTitleColor="{StaticResource MainBlue}">

    <TabBar>
        <Tab Title="Dashboard" Icon="dashboard_icon.png">
            <ShellContent
                Title="Dashboard"
                ContentTemplate="{DataTemplate views:DashboardPage}"
                Route="DashboardPage" />
        </Tab>

        <Tab Title="Wycieczki" Icon="trips_icon.png">
            <ShellContent
                Title="Moje Wycieczki"
                ContentTemplate="{DataTemplate views:TripListPage}"
                Route="TripListPage" />
        </Tab>

        <Tab Title="Konto" Icon="account_icon.png">
            <ShellContent
                Title="Moje Konto"
                ContentTemplate="{DataTemplate views:AccountPage}"
                Route="AccountPage" />
        </Tab>
    </TabBar>

</Shell>