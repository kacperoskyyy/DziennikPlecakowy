<?xml version="1.0" encoding="utf-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:DziennikPlecakowy.Views"
    x:Class="DziennikPlecakowy.AppShell">

    <FlyoutItem Title="Wycieczki" Icon="icon_trip.png">
        <ShellContent Title="Lista" ContentTemplate="{DataTemplate views:TripsListPage}" />
        <ShellContent Title="Nowa" ContentTemplate="{DataTemplate views:TripPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Profil" Icon="icon_profile.png">
        <ShellContent Title="Mój profil" ContentTemplate="{DataTemplate views:ProfilePage}" />
    </FlyoutItem>

    <FlyoutItem Title="Statystyki" Icon="icon_stats.png">
        <ShellContent Title="Statystyki" ContentTemplate="{DataTemplate views:StatsPage}" />
    </FlyoutItem>

    <MenuItem Text="Wyloguj" Clicked="OnLogoutClicked" IconImageSource="icon_logout.png" />
</Shell>
